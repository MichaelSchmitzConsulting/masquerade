# Masquerade Service Response Simulator Default Channels
Includes the default channels available in Masquerade. These are

* HTTP (through servlet container)
* HTTP (standalone on different ports)
* File
* FTP